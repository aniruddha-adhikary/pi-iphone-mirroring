# Embedded iPhone Mirroring MCP server

This Python package is embedded in the parent Pi package. It exposes the
vision-based iPhone Mirroring tools over stdio and is launched automatically by
`extensions/iphone-mirroring.ts`.

Run directly for development with:

```bash
uv run --frozen macos-mcp
```
